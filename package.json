{
  "name": "octopus-ai",
  "version": "1.0.0",
  "description": "🐙 Octopus AI - Desktop App",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently -k -n server,client,electron \"node server/supervisor.js\" \"cd client && npm run dev\" \"wait-on http://localhost:5173 http://localhost:3001/api/health && electron .\"",
    "test": "cd server && npm test && cd ../client && npm test",
    "lint": "cd client && npm run lint",
    "build": "cd client && npm run build",
    "check": "npm run test && npm run lint && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vscode/test-electron": "^2.5.2",
    "concurrently": "^8.2.0",
    "dotenv": "^16.0.0",
    "electron": "^28.3.3",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "eslint": "^10.4.0",
    "lodash.merge": "^4.6.2",
    "prettier": "^3.8.3",
    "unzipper": "^0.12.3"
  }
}
